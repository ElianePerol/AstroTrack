<Window x:Class="AstroTrack.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:AstroTrack.UI.ViewModels"
        Title="AstroTrack" Height="450" Width="800">
    <Window.DataContext>
        <vm:EventsViewModel/>
    </Window.DataContext>

    <Grid Margin="10">
        <DataGrid ItemsSource="{Binding Events}"
              SelectedItem="{Binding SelectedEvent}"
              AutoGenerateColumns="False"
              CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nom"    Binding="{Binding Name}" Width="2*"/>
                <DataGridTextColumn Header="Date"   Binding="{Binding Date, StringFormat=d}" Width="*"/>
                <DataGridTextColumn Header="Type"   Binding="{Binding Type}" Width="*"/>
                <DataGridTextColumn Header="Desc."  Binding="{Binding Description}" Width="3*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
